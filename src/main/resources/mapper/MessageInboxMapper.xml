<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epsilon.welink.message.mapper.MessageInboxMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO message_inbox (msg_id, receiver_id, conversation_type, status, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.msgId}, #{item.receiverId}, #{item.conversationType}, #{item.status}, NOW(), NOW())
        </foreach>
    </insert>

</mapper>
