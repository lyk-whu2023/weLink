✅ 项目已全部跑起来！
服务状态总览
服务	端口	状态
MySQL (Docker)	3307	🟢 healthy
Redis (Docker)	6379	🟢 healthy
Kafka (Docker)	9092	🟢 running
MinIO (Docker)	9000/9001	🟢 healthy
Spring Boot 后端	8080/8081	🟢 Started (3.3s)
Vue 前端	3000	🟢 ready
API 实测
POST /api/v1/auth/register  → 200 success ✅
POST /api/v1/auth/login     → 200 success + JWT token ✅
数据库（7 张表已初始化）
user, friend_relation, group_info, group_member,
message, message_inbox, message_outbox
访问地址
- 前端页面: http://localhost:3000
- MinIO 控制台: http://localhost:9001 (minioadmin/minioadmin)
过程中修复的问题
1. npm 淘宝镜像证书过期 → 切到官方 registry.npmjs.org
2. 3306 端口被 Windows MySQL80 占用 → Docker MySQL 改用 3307
3. MySQL 8.0 Public Key Retrieval 错误 → JDBC URL 加了 allowPublicKeyRetrieval=true
4. Kafka bitnami 镜像 tag 不存在 → 改用本地已有的 apache/kafka:3.8.0（KRaft 模式，省掉 Zookeeper）
后续使用
# 中间件启停
docker compose -f G:\weLink\docker-compose.yml up -d     # 启动
docker compose -f G:\weLink\docker-compose.yml down       # 停止
# IDEA 启动后端
# 打开 G:\weLink → Run WeLinkApplication
# 前端
cd G:\weLink\frontend
npm run dev
你现在浏览器打开 http://localhost:3000 就能看到界面了。