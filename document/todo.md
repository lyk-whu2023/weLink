考虑baseentity中时间的填充问题
考虑自服务的controller和service的调用问题与事务问题
重点关注IMService的实现，确保消息的正确处理和分发
群发消息实现有问题，需要检查


注意配置redis、kafka、mysql等的密码
# 在 WSL 中修改 Kafka 配置文件
# 找到 kafka 的 server.properties,添加:
advertised.listeners=PLAINTEXT://localhost:9092


因果一致性，消息重发保证消息不丢失，数据库存储方式、选型，消息具体实现（文档编写），

考虑先落库在投递对延迟的影响，是否需要优化

出现前端在线状态显示问题
这里增加后端上线广播功能和前端上线检测功能

出现登录没有正确拉取消息的问题
这里增加登录后，立即拉取会话摘要（包含各个会话的未读消息数等）的功能，
会话摘要中包含每个会话的未读消息数、最后一条消息的发送者、最后一条消息的内容等。
前端根据会话摘要，更新会话列表，显示未读消息数和最后一条消息。
前端点击会话列表项，加载会话详情，显示会话历史消息。
前端点击会话详情中的消息，加载消息内容。
